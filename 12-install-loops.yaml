- name: installing packages using loops
  hosts: localhost
  connection: local
  tasks:
  - name : loops 
    ansible.builtin.package:
      name: "{{item}}"
      state: present
    loop:
    - nginx
    - mysql
    - zip 